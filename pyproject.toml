[project]
name = "earth-photo-manager"
version = "0.1.0"
description = "PySide6 photo manager for browsing registered image folders"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cx-Freeze>=7.0",
    "PySide6>=6.6",
]
